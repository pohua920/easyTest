<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="Utils" >

  	<sql id="prpinsSchema"></sql>
  	<sql id="msSqlMobSchema"></sql>
  	<sql id="msSqlShSchema"></sql>
  	<sql id="msSqlSmsSchema"></sql>
  	<sql id="dmsSchema"></sql>
  	<sql id="db2CommData">TSTCOMMDAT.</sql><!-- 測試是  TSTCOMMDAT 正式是 COMMDATA-->
  	<sql id="db2Oracle">TSTORACLE.</sql><!-- 測試是  TSTORACLE 正式是 ORACLE--><!-- mantis：REI00021，處理人員：DP0706，需求單編號：REI00021 再保帳單介接400-->
  	<sql id="xchgSchema"></sql>
  	<sql id="salesSchema"></sql>
	<sql id="schema"></sql>
  	<sql id="sql1">SELECT * FROM (SELECT A.*, ROWNUM RN FROM ( (</sql>
	<sql id="sql2"> ORDER BY  $sortBy$ $sortType$ ) A ))WHERE RN BETWEEN $startRow$ AND $endRow$</sql>
	
	<sql id="msSql1">SELECT * FROM(SELECT A.*, ROW_NUMBER() OVER(ORDER BY $sortBy$ $sortType$ ) AS ROWID FROM(</sql>
	<sql id="msSql2">) AS A) AS B WHERE ROWID BETWEEN $startRow$ AND $endRow$</sql>
</sqlMap>